package gaj.analysis.bayes;

/**
 * Denotes any probability distribution, p(x). 
 * <br/>For a discrete domain X, 0 <= p(x) <= 1 and sum_{x in X} p(x) = 1. 
 * <br/>For a continuous domain, p(x) >= 0 and int_{X} p(x) dx = 1.
 */
public interface ProbDist {

}
