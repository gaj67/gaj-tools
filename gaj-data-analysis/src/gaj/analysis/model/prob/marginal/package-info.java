/**
 * Specifies marginal probability models of arbitrary input data, x, which
 * produce marginal probabilities, p(x) = sum_{y} p(x,y), for discrete, latent
 * class or category labels, y.
 */
package gaj.analysis.model.prob.marginal;