/**
 * Specifies discriminative probability models of arbitrary input data, x, which
 * produce posterior probabilities, p(y|x), of discrete, latent class or
 * category labels, y.
 */
package gaj.analysis.model.prob.discriminative;