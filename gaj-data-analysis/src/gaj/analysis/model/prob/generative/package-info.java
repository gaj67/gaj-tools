/**
 * Specifies generative probability models of arbitrary input data, x, which
 * produce conditional probabilities, p(x|y), of discrete, latent class or
 * category labels, y.
 */
package gaj.analysis.model.prob.generative;